// Subset of raceoverlay/src/lib/constants.js — only the values the
// backend renderer needs. Keep this in sync with the frontend file.

/** Conversion factor: 1 km/h = 0.621371 mph */
export const KPH_TO_MPH = 0.621371;
